spring:
    application:
        name: dashboard
    cloud:
        config:
            fail-fast: true
            uri: http://admin:admin@localhost:8761/config
            label: master
            name: dashboard
            profile: prod
